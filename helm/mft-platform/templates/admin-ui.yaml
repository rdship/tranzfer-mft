{{- if .Values.adminUi.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-ui
  namespace: {{ .Release.Namespace }}
  labels: {{- include "mft.labels" . | nindent 4 }}
    app: admin-ui
spec:
  replicas: {{ .Values.adminUi.replicaCount }}
  selector:
    matchLabels:
      app: admin-ui
  template:
    metadata:
      labels:
        app: admin-ui
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: admin-ui
          image: "{{ .Values.global.imageRegistry }}{{ .Values.adminUi.image.repository }}:{{ .Values.global.imageTag }}"
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: admin-ui
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.adminUi.service.type }}
  ports:
    - port: {{ .Values.adminUi.service.port }}
      targetPort: 80
  selector:
    app: admin-ui
{{- end }}

{{- if .Values.ftpWebUi.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftp-web-ui
  namespace: {{ .Release.Namespace }}
  labels: {{- include "mft.labels" . | nindent 4 }}
    app: ftp-web-ui
spec:
  replicas: {{ .Values.ftpWebUi.replicaCount }}
  selector:
    matchLabels:
      app: ftp-web-ui
  template:
    metadata:
      labels:
        app: ftp-web-ui
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ftp-web-ui
          image: "{{ .Values.global.imageRegistry }}{{ .Values.ftpWebUi.image.repository }}:{{ .Values.global.imageTag }}"
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ftp-web-ui
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.ftpWebUi.service.type }}
  ports:
    - port: {{ .Values.ftpWebUi.service.port }}
      targetPort: 80
  selector:
    app: ftp-web-ui
{{- end }}
